# Render Deployment Fix Progress - UPDATED ✅\n\n## ✅ Step 1: Diagnose Issue (Complete)\n\n- **Root Cause**: MONGO_URI has `&amp;w=majority` (HTML-encoded) instead of `&w=majority`\n- Server.js fixed: dotenv now works on Render + local\n\n## ✅ Step 2: .env.example (Updated)\n\nCopy backend/.env.example → backend/.env (local dev):\n`\nMONGO_URI=... # Use & not &amp;\n`\n\n## ✅ Step 3: .gitignore (Good)\n\nAlready ignores .env\*\n\n## 🚀 Step 4: Fix Render Environment Variable **CRITICAL**\n\n1. https://dashboard.render.com → your service → Environment\n2. Edit `MONGO_URI`: Replace **ALL** `&amp;` → `&` (e.g. `&amp;w=majority` → `&w=majority`)\n3. **Save** → **Manual Deploy** or git push\n\n**Atlas tip**: When copying connection string, paste to notepad first, replace &amp; → &\n\n## ✅ Step 5: Test\n\n- Render logs: `MongoDB Connected: cluster...`\n- API test: `https://your-app.onrender.com/api/posts`\n\n## Step 6: Seed Data\n\n`bash\ncd backend\nnpm run seed  # admin@folio.com\nnode seedPosts.js\n`\n\n**Deploy ready! Fix env var → redeploy → success!** 🎉\n\nSee TODO-RENDER-FIX.md for full tracker.
